import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {
    gridRow: 2,
    width: '90%',
    margin: 'auto',
  },
  table: {
    width: '100%',
    minWidth: 300,
  },
});

export default useStyles;
